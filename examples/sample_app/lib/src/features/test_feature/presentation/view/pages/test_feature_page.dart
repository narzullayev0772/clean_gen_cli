import 'package:flutter/material.dart';

class TestFeaturePage extends StatelessWidget {
  const TestFeaturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TestFeature Page'),
      ),
    );
  }
}
