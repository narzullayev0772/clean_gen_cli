name: clean_gen_cli
description: A powerful CLI tool for Flutter developers to generate Clean Architecture features from JSON or YAML configuration files.
version: 1.2.1
homepage: https://github.com/narzullayev0772/clean_gen_cli
repository: https://github.com/narzullayev0772/clean_gen_cli.git

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  yaml: ^3.1.2
  path: ^1.9.0
  recase: ^4.1.0
  mason_logger: ^0.2.14

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  clean_gen: clean_gen
